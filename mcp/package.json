{
  "name": "applygo-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only Model Context Protocol server for ApplyGo",
  "bin": { "applygo-mcp": "src/server.js" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
